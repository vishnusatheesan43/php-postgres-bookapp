apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: bookcatalogapp  
spec:
  selector:
     app: postgres-deploy
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432

